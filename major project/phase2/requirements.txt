# Phase 2 requirements
# Core (same as Phase 1)
tensorflow==2.10.1
numpy<1.24
pandas
scikit-learn
pyyaml
joblib
matplotlib
seaborn

# Phase 2 additions — Transformer-based semantic branch
transformers>=4.30.0,<5.0.0  # 5.x dropped TF model support; pin to 4.x
tokenizers>=0.13.0,<0.20.0   # must match transformers 4.x pin
